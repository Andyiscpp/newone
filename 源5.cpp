#include<iostream>
#include <iomanip>
#include<string>
using namespace std;
/*int max(int x, int y) {
	int z;
	z = (x > y) ? x : y;
	return z;
}
int min(int x, int y) {
	int z;
	z = (x < y) ? x : y;
	return z;
}*/
/*int main()
{
	int a, b;
	cin >> a;
	cin >> b;
	int i = max(a,b);
	int ma = max(a, b);
	int mi = min(a, b);


	while (ma>= 1) {
		if (a % ma == 0 && b % ma == 0)
		{
			cout << "最大公因数为:" << ma;
			break;
		}
		else
		ma--;
	}
	int z = max(a, b);
	while (z >= 1) {
		if (a % z == 0 && b % z == 0)
		{
			cout<<"最小公倍数是" << a * b / z;
			break;
		}
		else
			z--;
	}
	*/


	/*while (i <= a && i <= b)
	{
		if (a % i == 0 && b % i == 0)
			if (mi % ma == 0)
				cout << "最小公倍数为" << ma << endl;
			else
		{
			int num = i;
			int m = max(a, b);
			cout << "最小公倍数为" << i * m << endl;break;
		}
		else
		{
			cout <<"最小公倍数是" << a * b << endl; break;
		}
		i++;
	}
	
return 0;
}*/